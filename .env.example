# === Réseau DMZ utilisé par macvlan ===
# Nom du réseau Docker macvlan (doit rester stable)
NET_NAME=macvlan_lan
# Sous-réseau et passerelle de la DMZ
SUBNET=10.10.0.0/24
GATEWAY=10.10.0.1

# === IP des backends (une IP par conteneur) ===
NODE1_IP=10.10.0.101
NODE2_IP=10.10.0.102

# === Shim macvlan côté hôte (pour tester depuis l'hôte backends) ===
HOST_SHIM_NAME=macvlan0
HOST_SHIM_IP=10.10.0.200
# Laissez vide pour auto-détection (route par défaut); sinon ex: enp0s3
PARENT_IF=
